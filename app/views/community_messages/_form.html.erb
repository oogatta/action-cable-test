<%= form_with(model: community_message, local: true) do |form| %>
  <% if community_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(community_message.errors.count, "error") %> prohibited this community_message from being saved:</h2>

      <ul>
      <% community_message.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :community_id %>
    <%= form.text_field :community_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
